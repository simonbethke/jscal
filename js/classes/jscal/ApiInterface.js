define([ 'class'], function(Class) {
  var ApiInterface = Class.extend({
    
    /*
     var input = {
       period: {
          start: utc timestamp,
          end: utc timestamp
       },
       success: function(eventArray) {},
       error: function(errorMsg) {}
     }
    */
    getEvents : function(input) {
      
    }
    
  });

  return ApiInterface;
});
